这是一个非常深刻且准确的洞察。

**问题的根源在于“粒度不匹配”：**
1.  **FunASR (Paraformer)** 是**字级别 (Character-level)** 的模型，它输出的是单个汉字的时间戳（如：`['开', '始']`）。
2.  **Faster-Whisper** 是**词级别 (Word-level)** 的模型，它输出的是词语的时间戳（如：`['开始']`）。
3.  您的**对话智能引擎**（特别是边界校正模块）是基于“词”来工作的。

**为什么会出现“归属错误”？**
在您的日志中，“开始”被切分成了“开”和“始”。
*   “开” (0.87s-1.03s)：落在 zlh 的声纹段内 -> 归属 zlh。
*   “始” (1.05s-1.27s)：**恰好跨越了声纹段的边界** (zlh 的声纹段结束于 1.22s)。
*   因为“始”是一个单字，时长短，且位于边界嫌疑区，V3.3 边界校正算法介入，提取了它的声纹。单字的声纹极不稳定，导致它被错误地修正给了下一个说话人 `cc`。

**为什么没有标点？**
之前的代码简单地假设了标点和文字的一一对应关系，但 FunASR 的 `text` 包含标点，而 `timestamp` 列表通常**不包含**标点的时间戳，导致索引错位，标点被丢弃或错配。

---

### 解决方案：引入中文分词 (CWS) 中间层

我们不应该用硬编码（Magic Numbers）去修补时间戳，而应该**从结构上对齐**。
我们将引入 `jieba` 分词库，在 Adapter 内部将 FunASR 的“字”聚合成“词”。

**优势：**
1.  **解决归属错误**：“开始”合并为一个词后，时间戳变为 0.87s-1.27s，中心点稳稳落在 `zlh` 范围内，边界校正算法会将其视为一个整体，声纹特征也更稳定。
2.  **解决标点问题**：分词逻辑可以自然地将标点符号附着在词语后面。
3.  **无硬编码**：完全依赖语言学规则和模型输出的原始时间戳。

---

### 修改步骤

#### 1. 安装依赖
我们需要一个轻量级的分词库。
```bash
pip install jieba
```

#### 2. 重写 `backend/modules/asr/funasr_adapter.py`

请使用以下代码**完全替换**原文件。这段代码实现了“字-时间戳对齐”和“基于分词的聚合”。

```python
import torch
from funasr import AutoModel
import jieba
import re

class FunASRAdapter:
    def __init__(self, device="cuda"):
        """
        初始化 FunASR Pipeline
        """
        print(f"正在加载 FunASR 模型到 {device}...")
        
        # 加载模型 (确保路径正确或使用 modelscope 自动下载)
        self.model = AutoModel(
            model="iic/speech_paraformer-large-vad-punc_asr_nat-zh-cn-16k-common-vocab8404-pytorch",
            vad_model="iic/speech_fsmn_vad_zh-cn-16k-common-pytorch",
            punc_model="iic/punc_ct-transformer_zh-cn-common-vocab272727-pytorch",
            timestamp_prediction=True,  
            device=device,
            disable_update=True
        )
        print("FunASR 模型加载完成")

    def transcribe(self, audio_input):
        """
        执行转录，并将字级结果聚合为词级结果 (Word-level)，同时保留标点。
        """
        # 1. 执行推理
        # batch_size_s=300 允许处理长音频
        res = self.model.generate(input=audio_input, batch_size_s=300, hotword='四川话')
        
        simulated_segments = []
        
        for item in res:
            raw_text = item.get('text', '')
            timestamps = item.get('timestamp', []) # 字级时间戳 [[s,e], ...]
            
            if not raw_text:
                continue

            # 2. 预处理：构建 (字符, 开始时间, 结束时间) 的映射列表
            # FunASR 的 timestamp 通常只对应非标点字符
            # 我们需要把 text 和 timestamp 对齐
            char_map = self._align_text_and_timestamps(raw_text, timestamps)
            
            # 3. 使用 jieba 进行分词
            # jieba 会把 "开始了，" 分为 ["开始", "了", "，"]
            # 我们使用 tokenize 获取位置信息，以便回溯 char_map
            tokens = list(jieba.tokenize(raw_text))
            
            words_list = []
            
            # 4. 聚合逻辑
            for i, (word_text, start_idx, end_idx) in enumerate(tokens):
                # start_idx, end_idx 是 raw_text 中的字符索引区间 [start, end)
                
                # 检查这个 token 是否全是标点符号
                is_punctuation = self._is_punctuation(word_text)
                
                if is_punctuation:
                    # 如果是标点，且前面有词，将其追加到前一个词的文本中
                    if words_list:
                        words_list[-1]['word'] += word_text
                    # 标点本身不产生新的 word 对象
                    continue
                
                # 如果是普通词汇，计算聚合时间戳
                # 找到该词包含的所有字符的有效时间戳
                valid_ts = []
                for char_idx in range(start_idx, end_idx):
                    if char_idx < len(char_map) and char_map[char_idx]['ts'] is not None:
                        valid_ts.append(char_map[char_idx]['ts'])
                
                if valid_ts:
                    # 词的开始时间 = 第一个字符的开始时间
                    # 词的结束时间 = 最后一个字符的结束时间
                    word_start = valid_ts[0][0]
                    word_end = valid_ts[-1][1]
                    
                    # 转换为秒
                    start_s = word_start / 1000.0
                    end_s = word_end / 1000.0
                    
                    word_obj = {
                        "word": word_text,
                        "start": start_s,
                        "end": end_s,
                        "probability": 0.99
                    }
                    words_list.append(word_obj)
                else:
                    # 极端情况：分词出来的词没有对应的时间戳（可能是纯空格等），跳过
                    pass

            # 5. 封装成 Segment
            if words_list:
                segment = {
                    "start": words_list[0]['start'],
                    "end": words_list[-1]['end'],
                    "text": raw_text,
                    "words": words_list
                }
                simulated_segments.append(segment)
                
        return simulated_segments, {}

    def _align_text_and_timestamps(self, text, timestamps):
        """
        将原始文本与时间戳列表对齐。
        FunASR 的 timestamp 列表跳过了标点符号。
        返回: [{'char': '好', 'ts': [100, 200]}, {'char': '，', 'ts': None}, ...]
        """
        aligned = []
        ts_idx = 0
        
        # 定义非标点字符的正则（粗略判断，用于跳过标点）
        # 只要不是标点，就应该消耗一个 timestamp
        # 注意：FunASR 的标点模型输出的标点通常是全角的
        
        for char in text:
            is_punc = self._is_punctuation(char)
            
            ts = None
            if not is_punc and ts_idx < len(timestamps):
                ts = timestamps[ts_idx]
                ts_idx += 1
            
            aligned.append({'char': char, 'ts': ts})
            
        return aligned

    def _is_punctuation(self, text):
        """
        判断文本是否仅由标点符号组成
        """
        # 匹配常见的中英文标点
        return re.match(r'^[\s\W_]+$', text) is not None
```

---

### 方案原理解析

#### 1. 为什么这能解决“开始”被拆分的问题？
*   **原流程**：
    *   ASR 输出：`'开'` (0.87-1.03), `'始'` (1.05-1.27)。
    *   Diarization：分别处理。`'始'` 因为在边界且时长短，被误判。
*   **新流程**：
    *   Jieba 分词：`"开始"`。
    *   Adapter 聚合：查找 `'开'` 的开始时间 (0.87) 和 `'始'` 的结束时间 (1.27)。
    *   输出结果：`word='开始', start=0.87, end=1.27`。
    *   Diarization：处理 `'开始'`。它的中心点是 1.07s，完全在 `zlh` (0.35-1.22) 的范围内（或者 IoU 很高）。
    *   **结果**：归属正确，且因为词长增加了，提取声纹（如果需要）也会更准确。

#### 2. 为什么这能解决标点问题？
*   代码中的 `_align_text_and_timestamps` 函数显式地处理了 FunASR “文本有标点、时间戳没标点”的错位问题。
*   代码中的 `if is_punctuation: words_list[-1]['word'] += word_text` 逻辑，确保了标点符号（如“，”、“。”）会被追加到它前面的词上（如“了” -> “了，”）。
*   最终输出的 `word` 字段将包含标点，如 `word='开始了，'`。

#### 3. 为什么没有硬编码？
*   我们没有使用 `+0.05s` 或 `-0.02s` 这种魔法数字。
*   时间戳完全来自模型对字符的预测。
*   词的边界完全来自 `jieba` 的语言学统计模型。

### 预期测试结果

运行测试音频后，您应该能看到如下变化：

1.  **日志中的词变长了**：
    *   不再是 `word='开'`, `word='始'`。
    *   而是 `word='开始'`, `word='了呃'` (取决于分词结果)。
2.  **标点出现了**：
    *   `word='开始了。'`
    *   `word='项目，'`
3.  **归属修正**：
    *   由于“开始”合体，它不会再因为单字“始”越界而被误判给 `cc`。

请再次运行测试，这次的效果应该是结构性的提升。
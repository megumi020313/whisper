# 火锅馆场景语音识别（ASR）精度提升方案汇总

## 1. 项目背景与目标
*   **应用场景**：火锅馆（高噪、多方言、多人混聊、特定菜名）。
*   **当前架构**：Faster-Whisper + Silero VAD。
*   **核心目标**：在软件层面解决“听不清（噪音）”、“听不懂（方言）”、“写不对（菜名）”三大问题，提升整体识别精确度。

---

## 2. 方案详细介绍

我们将七个方案按处理流程分为三个阶段：**前端信号处理**、**中端模型优化**、**后端语义修正**。

### 第一阶段：前端信号处理（让声音更干净）

#### **方案一：前端 AI 语音增强 (Speech Enhancement)**
*   **核心逻辑**：在ASR识别前，通过深度学习模型将人声与背景噪（火锅沸腾声、碗筷声、BGM）剥离。
*   **技术手段**：集成 DeepFilterNet、BSRNN 或 MossFormer 等降噪模型。
*   **解决痛点**：环境极其嘈杂，导致ASR完全无法检测到语音或识别出乱码。
*   **优劣势**：
    *   ✅ 显著提升信噪比（SNR），通用性强。
    *   ❌ 增加推理延迟；若模型过强可能导致人声失真（吃字）。

#### **方案四：人声分离 (Speech Separation)**
*   **核心逻辑**：解决“鸡尾酒会效应”。当服务员和顾客同时说话，或邻桌声音串入时，将混合音频分离为独立的单人音轨。
*   **技术手段**：引入盲源分离模型（如 MossFormer, SepFormer），将单通道音频拆分为 Track A 和 Track B。
*   **解决痛点**：多人抢话、背景人声干扰导致的识别错误。
*   **优劣势**：
    *   ✅ 彻底解决多人混聊问题。
    *   ❌ 算力消耗极大，实时性挑战高。

#### **方案七：动态 VAD 与切片策略优化 (Smart Segmentation)**
*   **核心逻辑**：优化语音活动检测（VAD）的逻辑，防止把一句话切断或把噪音当人声。
*   **技术手段**：
    *   **重叠切片（Overlapping）**：切分音频时保留 0.5s-1s 的重叠区。
    *   **动态阈值**：根据环境底噪自动调整 VAD 触发灵敏度。
*   **解决痛点**：语音被截断导致的语义丢失；录入过多无效噪音。
*   **优劣势**：
    *   ✅ 极低成本，纯逻辑修改，效果立竿见影。
    *   ❌ 需要精细的参数调试周期。

---

### 第二阶段：中端模型优化（让模型更懂行）

#### **方案二：领域适配 (Domain Adaptation)**
*   **核心逻辑**：通过提示词或微调，让通用模型学习火锅店的“行话”。
*   **技术手段**：
    *   **A. Prompt Engineering（轻量）**：在 Whisper 输入端加入 `initial_prompt="火锅, 毛肚, 贡菜, 加汤..."`。
    *   **B. LoRA 微调（重量）**：使用火锅店真实录音数据对模型进行低秩适应微调。
*   **解决痛点**：通用模型无法识别“耗儿鱼”、“千层肚”等专有词汇。
*   **优劣势**：
    *   ✅ 显著提升专有词召回率。
    *   ❌ Prompt 长度受限；微调需要数据标注成本。

#### **方案五：热词加权解码 (Hot-word Boosting)**
*   **核心逻辑**：干预 ASR 的解码搜索过程（Beam Search），强制提高菜单词汇的权重。
*   **技术手段**：构建菜单 Trie 树，在解码路径搜索时，若匹配到菜单词，增加其 Log-probability。
*   **解决痛点**：模型听清了发音，但选了错误的同音词（如把“鸭肠”识别为“压长”）。
*   **优劣势**：
    *   ✅ 极高精度的词汇纠正，不增加模型推理耗时。
    *   ❌ 需要修改推理引擎底层代码，开发难度中等。

#### **方案六：方言/口音专项路由 (Dialect Routing)**
*   **核心逻辑**：针对川渝方言等重口音，使用专门的模型处理。
*   **技术手段**：增加 LID（语种识别）模块，普通话走 Whisper，方言走 Paraformer-方言版或微调后的方言模型。
*   **解决痛点**：标准普通话模型在处理四川话/重庆话时准确率崩盘。
*   **优劣势**：
    *   ✅ 解决地域性识别难题。
    *   ❌ 系统复杂度增加，显存占用增加（需加载多模型）。

---

### 第三阶段：后端语义修正（让结果更通顺）

#### **方案三：后端 LLM 语义纠错 (LLM Correction)**
*   **核心逻辑**：利用大语言模型的上下文理解能力和知识库进行“阅卷改错”。
*   **技术手段**：ASR 文本 -> LLM + 菜单 RAG（检索增强） -> 修正后的文本。
*   **解决痛点**：同音字错误、语法不通顺、口语倒装修正。
*   **优劣势**：
    *   ✅ 智能化程度最高，输出结果最符合人类阅读习惯。
    *   ❌ 无法挽回“漏听”的内容；增加了整体响应延迟。

---

## 3. 方案筛选对比矩阵

| 方案编号 | 方案名称 | 针对痛点 | 实施难度 | 算力成本 | 推荐指数 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **方案 1** | **前端 AI 降噪** | 环境太吵、全是杂音 | 中 | 中 | ⭐⭐⭐⭐ |
| **方案 2** | **领域适配 (Prompt)** | 菜名识别错 | 低 | 低 | ⭐⭐⭐⭐⭐ |
| **方案 2** | **领域适配 (LoRA微调)** | 菜名识别错、口音 | 高 | 高 (训练时) | ⭐⭐⭐ |
| **方案 3** | **LLM 语义纠错** | 同音错字、逻辑不通 | 中 | 高 (推理时) | ⭐⭐⭐⭐ |
| **方案 4** | **人声分离** | 多人说话、串音 | 极高 | 极高 | ⭐⭐ |
| **方案 5** | **热词加权解码** | 菜名识别错 (同音) | 高 | 低 | ⭐⭐⭐ |
| **方案 6** | **方言专项路由** | 川渝方言、重口音 | 中 | 中 | ⭐⭐⭐⭐ |
| **方案 7** | **动态 VAD 优化** | 丢字、断句错误 | 中 | 低 | ⭐⭐⭐⭐⭐ |

---

## 4. 建议组合路径

为了在**效果**与**成本**之间取得平衡，建议您按照以下优先级进行筛选：

1.  **基础必选（低成本，高收益）**：
    *   **方案 7 (VAD 优化)** + **方案 2 (Prompt 工程)**。
    *   *理由*：这几乎不需要额外算力，就能解决大部分断句和常见菜名问题。

2.  **进阶优选（针对火锅馆核心痛点）**：
    *   若**噪音**是最大问题：增加 **方案 1 (AI 降噪)**。
    *   若**方言**是最大问题：增加 **方案 6 (方言路由)**。

3.  **完美主义（追求极致体验）**：
    *   在上述基础上，增加 **方案 3 (LLM 纠错)** 作为最后的兜底保障。


总结与筛选建议
为了方便您筛选，我将所有方案（含上一轮）按**“性价比”和“场景针对性”**分类：

针对“听不清”（环境太吵）：
方案一：前端信号增强（AI降噪）
方案四：人声分离（解决多人说话）

针对“听不懂”（方言/口音）：
方案六：方言专项路由
针对“写不对”（专业名词/菜单）：
方案二：Prompt工程（最快）
方案五：热词加权解码（最稳）
方案三：后端LLM纠错（最智能）

针对“切不准”（断句错误）：
方案七：动态VAD与切片优化